---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.base.json
  - .gitignore
  - client/package.json
  - client/tsconfig.json
  - client/tsconfig.app.json
  - client/tsconfig.node.json
  - client/vite.config.ts
  - client/index.html
  - client/src/main.tsx
  - client/src/App.tsx
  - client/src/vite-env.d.ts
  - server/package.json
  - server/tsconfig.json
  - server/src/app.ts
autonomous: true

must_haves:
  truths:
    - "Root package.json declares workspaces for client and server"
    - "client/ is a working Vite + React + TypeScript app that starts with npm run dev"
    - "server/ has package.json with Express 5, Zod, dotenv, dd-trace, neo4j-driver, @aws-sdk/client-bedrock-runtime"
    - "server scripts bootstrap dd-trace via NODE_OPTIONS=\"--import dd-trace/initialize.mjs\""
    - "Shared tsconfig.base.json provides strict TypeScript config extended by both packages"
    - ".gitignore excludes .env, node_modules, and dist"
  artifacts:
    - path: "package.json"
      provides: "Root monorepo config with workspaces and concurrently dev script"
      contains: "workspaces"
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript compiler options"
      contains: "ES2023"
    - path: "client/package.json"
      provides: "Vite + React client package"
      contains: "vite"
    - path: "client/vite.config.ts"
      provides: "Vite config with /api proxy to Express"
      contains: "proxy"
    - path: "server/package.json"
      provides: "Express server package with all dependencies"
      contains: "@aws-sdk/client-bedrock-runtime"
    - path: "server/tsconfig.json"
      provides: "Server TypeScript config with NodeNext module resolution"
      contains: "NodeNext"
    - path: "server/src/app.ts"
      provides: "Express app factory function"
      contains: "createApp"
    - path: ".gitignore"
      provides: "Git exclusions for secrets and build artifacts"
      contains: ".env"
  key_links:
    - from: "client/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends"
      pattern: "extends.*tsconfig\\.base"
    - from: "server/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends"
      pattern: "extends.*tsconfig\\.base"
    - from: "client/vite.config.ts"
      to: "http://localhost:3001"
      via: "proxy config"
      pattern: "localhost:3001"
    - from: "package.json"
      to: "client and server workspaces"
      via: "npm workspaces + concurrently"
      pattern: "concurrently"
---

<objective>
Initialize the npm workspaces monorepo with client (Vite + React + TypeScript) and server (Express 5 + TypeScript) packages, shared tsconfig, and .gitignore. Also wire dd-trace bootstrap into server scripts from day 1.

Purpose: Establish the project skeleton so Plan 02 can add config validation, health route, and server entry point into an already-working structure.
Output: Root package.json with workspaces, client/ with Vite scaffolding and proxy config, server/ with package.json and Express app factory, shared tsconfig.base.json, .gitignore.
</objective>

<execution_context>
@/Users/blaisealbuquerque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blaisealbuquerque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffold/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo root, client Vite scaffold, and .gitignore</name>
  <files>
    package.json
    tsconfig.base.json
    .gitignore
    client/package.json
    client/tsconfig.json
    client/tsconfig.app.json
    client/tsconfig.node.json
    client/vite.config.ts
    client/index.html
    client/src/main.tsx
    client/src/App.tsx
    client/src/vite-env.d.ts
  </files>
  <action>
    1. Create root `package.json`:
       - `"name": "aws-hackathon"`, `"private": true`
       - `"workspaces": ["client", "server"]`
       - scripts: `"dev": "concurrently --names CLIENT,SERVER --prefix-colors cyan,yellow \"npm run dev -w client\" \"npm run dev -w server\""` and `"build": "npm run build -w client && npm run build -w server"`
       - devDependencies: `concurrently: "^9.2.1"`, `typescript: "^5.0.0"`

    2. Create `tsconfig.base.json` (shared):
       - `target: "ES2023"`, `strict: true`, `esModuleInterop: true`, `skipLibCheck: true`, `forceConsistentCasingInFileNames: true`, `declaration: true`, `declarationMap: true`, `sourceMap: true`

    3. Scaffold client using `npm create vite@latest client -- --template react-ts` (run from project root). This generates client/package.json, client/tsconfig.json, client/tsconfig.app.json, client/tsconfig.node.json, client/vite.config.ts, client/index.html, and client/src/ files.
       - After scaffold, update `client/vite.config.ts` to add proxy config:
         ```
         server: {
           port: 5173,
           proxy: {
             "/api": {
               target: "http://localhost:3001",
               changeOrigin: true,
             },
           },
         }
         ```
       - Replace the default App.tsx content with a minimal placeholder:
         ```tsx
         function App() {
           return <div><h1>AI Dungeon Master</h1><p>Scaffold ready.</p></div>;
         }
         export default App;
         ```
       - Update `client/tsconfig.json` to add `"extends": "../tsconfig.base.json"` and remove any compiler options already covered by the base (e.g., `strict`, `target`) so the shared config is the single source of truth for both packages.
       - Remove any default CSS files (App.css, index.css) and their imports from main.tsx — Phase 2 will add the dark fantasy theme from scratch.

    4. Create `.gitignore` at project root:
       ```
       .env
       .env.local
       node_modules/
       dist/
       ```

    5. Do NOT run `npm install` yet — Task 2 creates the server package.json first, then we install everything at once from root.
  </action>
  <verify>
    - `cat package.json` shows workspaces array with client and server
    - `cat tsconfig.base.json` shows target ES2023 and strict true
    - `cat client/vite.config.ts` shows proxy config for /api -> localhost:3001
    - `cat .gitignore` includes .env and node_modules
    - `ls client/src/` shows main.tsx and App.tsx
  </verify>
  <done>Root monorepo package.json exists with workspaces, tsconfig.base.json is shared, client/ is scaffolded with Vite + React + TS and /api proxy, .gitignore excludes .env and node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Create server package and Express app factory, then install all dependencies</name>
  <files>
    server/package.json
    server/tsconfig.json
    server/src/app.ts
  </files>
  <action>
    1. Create `server/package.json`:
       - `"name": "server"`, `"private": true`, `"type": "module"`
       - scripts: `"dev": "NODE_OPTIONS='--import dd-trace/initialize.mjs' tsx watch src/index.ts"`, `"build": "tsc"`, `"start": "NODE_OPTIONS='--import dd-trace/initialize.mjs' node dist/index.js"`
       - dependencies: `express: "^5.0.0"`, `zod: "^4.0.0"`, `dotenv: "^17.0.0"`, `dd-trace: "^5.86.0"`, `neo4j-driver: "^6.0.0"`, `@aws-sdk/client-bedrock-runtime: "^3.0.0"` (LOCKED DECISION: must be @aws-sdk/client-bedrock-runtime, NOT @anthropic-ai/bedrock-sdk)
       - devDependencies: `tsx: "latest"`, `@types/node: "latest"`, `@types/express: "latest"`, `typescript: "^5.0.0"`

    2. Create `server/tsconfig.json`:
       - `"extends": "../tsconfig.base.json"`
       - compilerOptions: `"module": "NodeNext"`, `"moduleResolution": "NodeNext"`, `"moduleDetection": "force"`, `"outDir": "dist"`, `"rootDir": "src"`
       - include: `["src"]`

    3. Create `server/src/app.ts` — Express app factory:
       ```typescript
       import express, { type Express } from "express";
       import type { Driver } from "neo4j-driver";

       interface AppDeps {
         driver: Driver;
       }

       export function createApp(_deps: AppDeps): Express {
         const app = express();
         app.use(express.json());
         // Routes will be added in Plan 02
         return app;
       }
       ```
       NOTE: Use `.js` extensions in all imports per NodeNext resolution rules (no imports from other local files in this file yet, but establish the pattern).

    4. Run `npm install` from project root to install all workspace dependencies at once. This will hoist shared deps and link workspaces.

    5. Verify install succeeded: check that `node_modules/` exists at root and `node_modules/express` is accessible.
  </action>
  <verify>
    - `cat server/package.json` shows `dd-trace` and `@aws-sdk/client-bedrock-runtime` in dependencies
    - `cat server/package.json` shows `NODE_OPTIONS='--import dd-trace/initialize.mjs'` in `dev` and `start` scripts
    - `cat server/tsconfig.json` shows module: NodeNext and extends ../tsconfig.base.json
    - `cat server/src/app.ts` shows createApp function with Express factory pattern
    - `ls node_modules/express` exists (dependencies installed)
    - `ls node_modules/neo4j-driver` exists
    - `npx tsc --noEmit -p server` compiles without errors (TypeScript check)
  </verify>
  <done>Server package exists with all required dependencies including @aws-sdk/client-bedrock-runtime (locked), Express app factory is created, npm install succeeds from root, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. Root package.json has workspaces: ["client", "server"]
2. tsconfig.base.json exists with strict: true, ES2023
3. client/vite.config.ts has /api proxy to localhost:3001
4. server/package.json has type: "module" and all required dependencies (including `dd-trace`)
5. server/tsconfig.json extends base and uses NodeNext
6. .gitignore covers .env and node_modules
7. npm install from root succeeds
8. npx tsc --noEmit -p server passes
</verification>

<success_criteria>
- Both workspace packages exist with correct package.json files
- All dependencies install cleanly from root
- TypeScript compiles without errors for server
- Vite client scaffold exists with proxy config
- dd-trace is bootstrap-ready from server scripts via NODE_OPTIONS
- .gitignore prevents secrets from entering git
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-01-SUMMARY.md`
</output>
