---
phase: 11-architecture-audit
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/middleware/rateLimits.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "rateLimits.ts exports only musicLimiter — no dead chatLimiter or narrateLimiter exports"
    - "app.ts continues to import musicLimiter from rateLimits.ts without error"
    - "TypeScript compilation succeeds with no import errors across the server workspace"
    - "All existing rate limiting behavior is unchanged — /api/chat uses chatRateLimiter from rateLimiter.ts, /api/narrate uses narrateRateLimiter from rateLimiter.ts, /api/music uses musicLimiter from rateLimits.ts"
  artifacts:
    - path: "server/src/middleware/rateLimits.ts"
      provides: "musicLimiter only — dead chatLimiter and narrateLimiter removed"
      contains: "musicLimiter"
  key_links:
    - from: "server/src/middleware/rateLimits.ts"
      to: "server/src/app.ts"
      via: "import { musicLimiter }"
      pattern: "import.*musicLimiter.*rateLimits"
---

<objective>
Remove dead code from rateLimits.ts: delete the orphaned chatLimiter and narrateLimiter exports that are never imported by any module. Update the module comment to clarify that rateLimits.ts is solely responsible for the musicLimiter, while rateLimiter.ts (Phase 09, Redis-backed) handles chat and narrate rate limiting.

Purpose: Close 2 verification gaps where chatLimiter (60/min, conversationId) and narrateLimiter (10/min, conversationId) exist but are never wired to any route — creating confusion about which limiter is authoritative. The Phase 09 Redis-backed rateLimiter.ts limiters (chatRateLimiter 20/min userId, narrateRateLimiter 10/min userId) are the correct, production-grade implementations already wired in app.ts.

Output: A cleaned rateLimits.ts exporting only musicLimiter, with a clear module-level comment explaining the rate limiting architecture.
</objective>

<execution_context>
@/Users/blaisealbuquerque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blaisealbuquerque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-investigate-our-system-architecture-to-determine-architecture-improvements-to-get-us-to-a-more-ideal-perfect-state-for-future-iterations-for-this-product/11-01-SUMMARY.md
@server/src/middleware/rateLimits.ts
@server/src/middleware/rateLimiter.ts
@server/src/app.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead chatLimiter and narrateLimiter from rateLimits.ts</name>
  <files>server/src/middleware/rateLimits.ts</files>
  <action>
Rewrite server/src/middleware/rateLimits.ts to contain ONLY the musicLimiter export. Specifically:

1. Update the module-level JSDoc comment to explain the rate limiting architecture:
   - rateLimits.ts: musicLimiter only (MemoryStore, conversationId-keyed)
   - rateLimiter.ts: chatRateLimiter and narrateRateLimiter (Redis-backed, userId-keyed, created in Phase 09)
   - Explain why they are separate: music has no auth requirement so uses conversationId key; chat/narrate use authenticated userId key with Redis persistence

2. DELETE the entire chatLimiter export (lines 17-41 of current file) — it is never imported anywhere. Confirmed by grep: no file imports chatLimiter from rateLimits.

3. DELETE the entire narrateLimiter export (lines 43-67 of current file) — it is never imported anywhere. Confirmed by grep: no file imports narrateLimiter from rateLimits.

4. KEEP the musicLimiter export (lines 69-93 of current file) exactly as-is — it IS imported and wired in app.ts lines 15, 44-45.

5. Keep the imports: `rateLimit` from "express-rate-limit" and `logEvent` from "../services/logger.js" — both are used by musicLimiter.

The resulting file should be approximately 35-40 lines: imports, module comment, and the single musicLimiter export.
  </action>
  <verify>
Run these checks:
1. `grep -c "chatLimiter\|narrateLimiter" server/src/middleware/rateLimits.ts` should return 0
2. `grep -c "musicLimiter" server/src/middleware/rateLimits.ts` should return at least 1
3. `npx tsc --noEmit -p server/tsconfig.json` should succeed with no errors (confirms app.ts import of musicLimiter still resolves)
4. `grep "import.*musicLimiter.*rateLimits" server/src/app.ts` should still match (confirms app.ts wiring unchanged)
  </verify>
  <done>
rateLimits.ts exports only musicLimiter. The dead chatLimiter and narrateLimiter are gone. TypeScript compiles clean. The module comment clearly documents the rate limiting architecture split between rateLimits.ts (music) and rateLimiter.ts (chat/narrate).
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `npx tsc --noEmit -p server/tsconfig.json` passes
2. No dead exports: `grep -E "export.*(chatLimiter|narrateLimiter)" server/src/middleware/rateLimits.ts` returns nothing
3. musicLimiter still exported: `grep "export const musicLimiter" server/src/middleware/rateLimits.ts` returns a match
4. app.ts unchanged: `grep "musicLimiter" server/src/app.ts` still shows import and route wiring
5. rateLimiter.ts unchanged: `grep "chatRateLimiter\|narrateRateLimiter" server/src/middleware/rateLimiter.ts` still shows both exports
6. Server tests pass: `cd server && npx vitest run` — all 41 tests pass
</verification>

<success_criteria>
- rateLimits.ts contains only the musicLimiter export with a clear architecture comment
- No file in the codebase references chatLimiter or narrateLimiter from rateLimits.ts
- TypeScript compiles without errors
- All existing server tests pass
- The 2 verification gaps (truths #1 and #2 from 11-VERIFICATION.md) are resolved: the dead code causing confusion is removed, and the module documents that rateLimiter.ts is the authoritative source for chat/narrate rate limiting
</success_criteria>

<output>
After completion, create `.planning/phases/11-investigate-our-system-architecture-to-determine-architecture-improvements-to-get-us-to-a-more-ideal-perfect-state-for-future-iterations-for-this-product/11-06-SUMMARY.md`
</output>
