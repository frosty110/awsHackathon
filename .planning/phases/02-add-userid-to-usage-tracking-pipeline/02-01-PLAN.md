---
phase: 02-add-userid-to-usage-tracking-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/usage.ts
  - server/src/services/usageTracker.ts
  - server/src/__tests__/services/usageTracker.test.ts
autonomous: true
requirements:
  - USAGE-01
  - USAGE-02
  - USAGE-03

must_haves:
  truths:
    - "UsageEntry type includes an optional userId field"
    - "All four record* functions accept an optional userId parameter"
    - "getUserUsage(userId) returns a UsageSummary filtered to that user's entries"
    - "Existing tests still pass with no changes to their call signatures"
    - "New tests verify userId storage and getUserUsage filtering"
  artifacts:
    - path: "packages/shared-types/src/usage.ts"
      provides: "UsageEntry with userId?: string | null"
      contains: "userId"
    - path: "server/src/services/usageTracker.ts"
      provides: "Updated record* functions + getUserUsage"
      exports: ["recordBedrockUsage", "recordTtsUsage", "recordMusicUsage", "recordVideoUsage", "getUserUsage"]
    - path: "server/src/__tests__/services/usageTracker.test.ts"
      provides: "Test coverage for userId storage and getUserUsage"
      contains: "getUserUsage"
  key_links:
    - from: "server/src/services/usageTracker.ts"
      to: "packages/shared-types/src/usage.ts"
      via: "import UsageEntry"
      pattern: "import.*UsageEntry.*shared-types"
---

<objective>
Add `userId` field to the usage tracking type and service layer, plus a `getUserUsage` query function.

Purpose: Enable per-user cost attribution — prerequisite for Phase 3 Bear Lumen integration and per-user usage dashboards. All route-level call site changes are deferred to Plan 02.

Output: Updated `UsageEntry` type, updated `record*` function signatures, new `getUserUsage` function, passing tests.
</objective>

<execution_context>
@/Users/blaisealbuquerque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blaisealbuquerque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-add-userid-to-usage-tracking-pipeline/02-RESEARCH.md
@packages/shared-types/src/usage.ts
@server/src/services/usageTracker.ts
@server/src/__tests__/services/usageTracker.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add userId to UsageEntry type and update record functions</name>
  <files>
    packages/shared-types/src/usage.ts
    server/src/services/usageTracker.ts
  </files>
  <action>
1. In `packages/shared-types/src/usage.ts`, add `userId?: string | null;` to the `UsageEntry` interface, after the `conversationId` field. This must be optional to maintain backward compatibility with existing test fixtures and music/video records that have no user context.

2. Rebuild the shared-types package: run `yarn workspace @dnd-adventures/shared-types build` so that `dist/` reflects the updated type.

3. In `server/src/services/usageTracker.ts`, update all four `record*` functions:

   - `recordBedrockUsage`: Add `userId?: string | null` as the LAST parameter. In the `entries.push()` call, add `userId: userId ?? null`.
   - `recordTtsUsage`: Add `userId?: string | null` as the LAST parameter. In the `entries.push()` call, add `userId: userId ?? null`.
   - `recordMusicUsage`: Add `userId?: string | null` as the ONLY parameter (currently takes none). In the `entries.push()` call, add `userId: userId ?? null`.
   - `recordVideoUsage`: Add `userId?: string | null` as the ONLY parameter (currently takes none). In the `entries.push()` call, add `userId: userId ?? null`.

4. Add a new exported function `getUserUsage` mirroring `getConversationUsage`:
   ```typescript
   export function getUserUsage(userId: string): UsageSummary {
     return summarize(entries.filter((e) => e.userId === userId));
   }
   ```

5. Update the `export type { UsageEntry, UsageSummary }` re-export line if needed (it should still work as-is since UsageEntry is re-exported from shared-types).

IMPORTANT: All parameters are optional — existing callers that don't pass userId will continue to work without changes (TypeScript optional parameter backward compatibility). Do NOT import Express types into this service file.
  </action>
  <verify>
    <automated>cd /Users/blaisealbuquerque/Projects/awsHackathon && yarn workspace @dnd-adventures/shared-types build && npx tsc --noEmit -p server/tsconfig.json 2>&1 | head -30</automated>
    <manual>Verify that UsageEntry in dist/index.d.ts includes userId field</manual>
  </verify>
  <done>
    - UsageEntry type has `userId?: string | null` field
    - All four record* functions accept optional userId parameter
    - getUserUsage function exists and filters by userId
    - Shared-types package rebuilt successfully
    - Server TypeScript compiles clean (no errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add userId test coverage to usageTracker tests</name>
  <files>
    server/src/__tests__/services/usageTracker.test.ts
  </files>
  <action>
1. Import `getUserUsage` in the test file's import block (add it alongside the existing imports from `../../services/usageTracker.js`).

2. Add new test cases to the existing `recordBedrockUsage` describe block:

   - Test: "stores userId when provided"
     - Call `recordBedrockUsage('conv-1', 'chat', 100, 100, 'user-abc')`
     - Assert `internals.entries[0].userId` equals `'user-abc'`

   - Test: "defaults userId to null when omitted"
     - Call `recordBedrockUsage('conv-1', 'chat', 100, 100)` (no userId)
     - Assert `internals.entries[0].userId` equals `null`

3. Add a new test case to the existing `recordTtsUsage` describe block:

   - Test: "stores userId when provided"
     - Call `recordTtsUsage('conv-2', 500, 'user-xyz')`
     - Assert `internals.entries[0].userId` equals `'user-xyz'`

4. Add a new `describe('getUserUsage')` block:

   - Test: "returns usage summary filtered by userId"
     - Record entries: `recordBedrockUsage('c1', 'chat', 100, 100, 'user-A')`, `recordBedrockUsage('c2', 'chat', 200, 200, 'user-B')`, `recordTtsUsage('c3', 300, 'user-A')`
     - Call `getUserUsage('user-A')`
     - Assert `summary.entries` equals 2 (both user-A records)
     - Assert `summary.totalCostUsd` is greater than 0

   - Test: "returns zeroed summary for unknown userId"
     - Call `getUserUsage('nonexistent')`
     - Assert `summary.entries` equals 0
     - Assert `summary.totalCostUsd` equals 0

5. Ensure no changes to existing test assertions — they must continue to pass as-is (the optional userId parameter means existing record* calls without userId are still valid).
  </action>
  <verify>
    <automated>cd /Users/blaisealbuquerque/Projects/awsHackathon && npx vitest run server/src/__tests__/services/usageTracker.test.ts 2>&1</automated>
  </verify>
  <done>
    - All existing tests pass unchanged
    - New tests verify userId storage on recordBedrockUsage and recordTtsUsage
    - New tests verify getUserUsage filtering returns correct entries and handles unknown userId
    - At least 5 new test assertions covering userId behavior
  </done>
</task>

</tasks>

<verification>
1. `yarn workspace @dnd-adventures/shared-types build` succeeds
2. `npx tsc --noEmit -p server/tsconfig.json` compiles clean
3. `npx vitest run server/src/__tests__/services/usageTracker.test.ts` — all tests pass
4. `grep -n "userId" packages/shared-types/src/usage.ts` shows the field in UsageEntry
5. `grep -n "getUserUsage" server/src/services/usageTracker.ts` shows the function export
</verification>

<success_criteria>
- UsageEntry type includes `userId?: string | null`
- All four record* functions accept optional userId
- getUserUsage function exists and filters correctly
- All tests pass (existing + new)
- TypeScript compiles clean across the monorepo
</success_criteria>

<output>
After completion, create `.planning/phases/02-add-userid-to-usage-tracking-pipeline/02-01-SUMMARY.md`
</output>
