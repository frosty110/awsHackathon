---
phase: 07-voice-demo-polish
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - server/src/services/system-prompt.ts
  - .planning/DEMO-SCRIPT.md
autonomous: false

must_haves:
  truths:
    - "A system prompt file exists with explicit constraints keeping the DM in the Shattered Crown tavern"
    - "The system prompt includes negative constraints: no new locations, no breaking the 3-turn flow"
    - "The system prompt includes dice roll narration brackets (1-5 failure, 6-10 partial, 11-15 success, 16-20 great success)"
    - "Three pre-written player inputs are documented and match the demo script"
  artifacts:
    - path: "server/src/services/system-prompt.ts"
      provides: "DM system prompt with lore injection placeholder and scripted scenario constraints"
      exports: ["DM_SYSTEM_PROMPT", "buildSystemPrompt"]
    - path: ".planning/DEMO-SCRIPT.md"
      provides: "Updated demo script with exact player inputs and verification checklist"
  key_links:
    - from: "server/src/services/system-prompt.ts"
      to: "Bedrock chat route (Phase 4)"
      via: "imported as system prompt for ConverseStreamCommand"
      pattern: "DM_SYSTEM_PROMPT|buildSystemPrompt"
---

<objective>
Finalize the DM system prompt with demo-safe constraints and verify the 3-turn scripted demo scenario is fully documented.

Purpose: DEMO-01 requires the scripted 3-turn demo to work reliably. This plan creates the system prompt that Phase 4 will use and ensures the demo script matches the implementation.
Output: `server/src/services/system-prompt.ts` (ready for Phase 4 import), verified `.planning/DEMO-SCRIPT.md`.
</objective>

<execution_context>
@/Users/blaisealbuquerque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blaisealbuquerque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DEMO-SCRIPT.md
@.planning/phases/07-voice-demo-polish/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DM system prompt with demo constraints</name>
  <files>server/src/services/system-prompt.ts</files>
  <action>
Create `server/src/services/system-prompt.ts`:

Export `DM_SYSTEM_PROMPT` as a string constant — the base system prompt for the Dungeon Master. This is used by the Phase 4 chat route as the first message in the Bedrock conversation.

Content of the system prompt (use a template literal):

```
You are the Dungeon Master for a dark fantasy D&D 5e adventure set in the world of Ashwick.

## Setting
The Shattered Crown Tavern — a dimly lit, stone-walled establishment at the edge of a wilderness town plagued by goblin raids. A fire crackles in the hearth. The barkeep is Gorm, a gruff one-eared dwarf and ex-soldier.

## Current Scene
The player has just arrived at the tavern. Gorm has information about a stolen artifact — the Ring of Ashwick — and suspects goblins in the northern caves.

## Style
- Vivid, immersive narration in 2-4 sentences per response
- Address the player directly ("you see", "you hear")
- End each response with a prompt for action ("What do you do?")
- Use atmospheric sensory details (sound, light, smell)

## Dice Roll Narration
When the player provides a dice roll result (a number 1-20), narrate the outcome:
- 1-5: Dramatic failure with consequence
- 6-10: Partial success or near miss
- 11-15: Solid success
- 16-20: Great success with flair and spectacle
- 20 exactly: Critical hit — legendary moment

Always reference the actual number rolled ("Your roll of 14 finds its mark...").

## Constraints (STRICT)
- NEVER change the location. The ENTIRE adventure takes place in the Shattered Crown Tavern and its immediate entrance.
- NEVER introduce characters not mentioned in the lore context below.
- NEVER break the fourth wall or reference being an AI.
- NEVER refuse to engage with combat — narrate it dramatically.
- If the player asks about something outside the tavern, redirect: the outside world is dangerous and night has fallen.
- Keep responses concise — 2-4 sentences maximum. Do not monologue.
```

Also export a `buildSystemPrompt` function:
```typescript
export function buildSystemPrompt(loreContext?: string): string {
  if (!loreContext) return DM_SYSTEM_PROMPT;
  return `${DM_SYSTEM_PROMPT}\n\n## Lore Context (from knowledge graph)\n${loreContext}`;
}
```

This function is what Phase 4 and Phase 5 will call — Phase 4 uses it without lore, Phase 5 passes the Neo4j-retrieved lore string.
  </action>
  <verify>
Run `npx tsc --noEmit -p server/tsconfig.json` — zero errors.
Verify `server/src/services/system-prompt.ts` exports `DM_SYSTEM_PROMPT` and `buildSystemPrompt`.
Verify the system prompt contains "NEVER change the location" constraint.
Verify dice roll brackets are specified (1-5, 6-10, 11-15, 16-20).
  </verify>
  <done>
System prompt file exists with DM persona, setting description, style rules, dice roll narration brackets, and strict constraints preventing the DM from going off-script. buildSystemPrompt function ready for Phase 4/5 integration.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify demo script alignment</name>
  <files>.planning/DEMO-SCRIPT.md</files>
  <action>
Human verification checkpoint. What was built:
- DM system prompt in `server/src/services/system-prompt.ts` with:
  - Setting: Shattered Crown Tavern in Ashwick
  - NPC: Gorm the one-eared dwarf barkeep
  - Quest: Ring of Ashwick stolen, goblins in northern caves
  - Strict constraints: no location changes, no off-script characters
  - Dice roll narration brackets: 1-5 failure, 6-10 partial, 11-15 success, 16-20 great success
  - buildSystemPrompt() function for lore injection (Phase 5)

How to verify:
1. Read `server/src/services/system-prompt.ts` and verify the system prompt matches the demo script at `.planning/DEMO-SCRIPT.md`
2. Confirm the three demo turns are compatible:
   - Turn 1 player input ("I look around the tavern and approach the barkeep") should trigger tavern description + Gorm introduction
   - Turn 2 player input ("I ask the barkeep about the ring") should trigger quest reveal
   - Turn 3 player input ("A goblin bursts through the door! I draw my sword and attack!") should trigger combat + dice roll request
3. Confirm the system prompt's constraints would prevent common drift scenarios:
   - DM suggesting the player leave the tavern
   - DM introducing random NPCs not in the lore
   - DM refusing combat or breaking character

Resume signal: Type "approved" or describe issues with the system prompt.
  </action>
  <verify>User confirms system prompt aligns with demo script and 3-turn scenario.</verify>
  <done>System prompt verified by user as compatible with the 3-turn demo script. No drift risks identified.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p server/tsconfig.json` passes with zero errors
- `server/src/services/system-prompt.ts` exists with `DM_SYSTEM_PROMPT` and `buildSystemPrompt` exports
- System prompt contains tavern setting, Gorm NPC, dice brackets, and strict location constraints
- Demo script player inputs are compatible with the system prompt's scenario
</verification>

<success_criteria>
The system prompt is ready for Phase 4 integration. The scripted 3-turn demo scenario has a constrained system prompt that prevents off-script DM responses. The demo script document is verified against the implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-voice-demo-polish/07-03-SUMMARY.md`
</output>
