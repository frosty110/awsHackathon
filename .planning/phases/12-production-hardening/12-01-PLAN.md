---
phase: 12-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/middleware/rateLimiter.ts
  - server/src/app.ts
  - server/src/middleware/auth.ts
autonomous: true

must_haves:
  truths:
    - "/api/auth/register rejects with 429 after 3 requests in 1 minute from the same IP"
    - "/api/auth/login rejects with 429 after 10 requests in 1 minute from the same IP"
    - "JWT verify in requireAuth and optionalAuth uses the same dev-secret fallback as jwt.sign in routes/auth.ts"
    - "Auth works in development without setting JWT_SECRET env var"
  artifacts:
    - path: "server/src/middleware/rateLimiter.ts"
      provides: "registerLimiter and loginLimiter exports"
      exports: ["registerLimiter", "loginLimiter"]
    - path: "server/src/app.ts"
      provides: "Auth limiters wired before authRouter"
      contains: "registerLimiter"
    - path: "server/src/middleware/auth.ts"
      provides: "DEV_SECRET fallback on jwt.verify"
      contains: "DEV_SECRET"
  key_links:
    - from: "server/src/app.ts"
      to: "server/src/middleware/rateLimiter.ts"
      via: "import registerLimiter, loginLimiter"
      pattern: "import.*registerLimiter.*loginLimiter.*rateLimiter"
    - from: "server/src/app.ts"
      to: "/api/auth/register and /api/auth/login"
      via: "app.use path-specific middleware before authRouter"
      pattern: 'app\.use\("/api/auth/register", registerLimiter\)'
    - from: "server/src/middleware/auth.ts"
      to: "server/src/routes/auth.ts"
      via: "shared DEV_SECRET string for sign/verify consistency"
      pattern: "config\\.JWT_SECRET \\|\\| DEV_SECRET"
---

<objective>
Harden auth endpoints with IP-based rate limiting and fix the JWT secret inconsistency between sign and verify.

Purpose: Prevent registration spam (3/min) and credential stuffing (10/min) on auth routes. Fix the dev-secret fallback so JWT verify matches JWT sign — currently auth breaks in development without an explicit JWT_SECRET because verify gets an empty string while sign has the fallback.

Output: Two new rate limiter exports in rateLimiter.ts, wired in app.ts before authRouter. DEV_SECRET constant in auth.ts used by both requireAuth and optionalAuth.
</objective>

<execution_context>
@/Users/blaisealbuquerque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blaisealbuquerque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-production-hardening/12-RESEARCH.md
@server/src/middleware/rateLimiter.ts
@server/src/middleware/auth.ts
@server/src/app.ts
@server/src/routes/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth rate limiters to rateLimiter.ts</name>
  <files>server/src/middleware/rateLimiter.ts</files>
  <action>
Add two new exported rate limiters after the existing `narrateRateLimiter`:

1. `registerLimiter` — 3 requests per 60-second window per IP:
   - `windowMs: 60 * 1000`
   - `limit: 3`
   - `standardHeaders: "draft-7"`, `legacyHeaders: false`
   - `keyGenerator: (req) => req.ip ?? "unknown"` (IP-keyed, NOT userId-keyed — these are unauthenticated endpoints)
   - `store: createStore("rl:register:")` (distinct prefix from login)
   - `message: { error: "Too many registration attempts, slow down" }`

2. `loginLimiter` — 10 requests per 60-second window per IP:
   - `windowMs: 60 * 1000`
   - `limit: 10`
   - `standardHeaders: "draft-7"`, `legacyHeaders: false`
   - `keyGenerator: (req) => req.ip ?? "unknown"` (IP-keyed)
   - `store: createStore("rl:login:")` (distinct prefix from register)
   - `message: { error: "Too many login attempts, slow down" }`

Follow the exact pattern of chatRateLimiter and narrateRateLimiter. The only differences: these use `req.ip ?? "unknown"` directly (no userId check since auth hasn't happened yet), and they have different limits and prefixes.

Add JSDoc comments matching the style of existing limiters: explain the limit, what it prevents, and that auth endpoints are IP-keyed.
  </action>
  <verify>
`npx tsc --noEmit` in server/ passes (no type errors). Grep for `registerLimiter` and `loginLimiter` in rateLimiter.ts confirms both exported.
  </verify>
  <done>rateLimiter.ts exports registerLimiter (3/min per IP) and loginLimiter (10/min per IP) using the existing createStore() helper with distinct Redis prefixes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire auth limiters in app.ts and fix JWT verify fallback in auth.ts</name>
  <files>server/src/app.ts, server/src/middleware/auth.ts</files>
  <action>
**In server/src/app.ts:**

1. Update the import from `./middleware/rateLimiter.js` to also import `registerLimiter` and `loginLimiter`.

2. Move the auth limiter registration BEFORE `authRouter`. The current code mounts authRouter at step 5 with the comment "Auth routes — no rate limit". Change the structure to:
   - Step 5: Auth rate limiting — add:
     ```
     app.use("/api/auth/register", registerLimiter);
     app.use("/api/auth/login", loginLimiter);
     ```
   - Step 6: Auth routes — `app.use(authRouter)` (moved from step 5)
   - Renumber existing steps 6-8 to 7-9.

3. Update the step 5 comment from "Auth routes — no rate limit (login/register must always be accessible)" to "Auth route rate limiting — IP-keyed to prevent spam/stuffing".

**In server/src/middleware/auth.ts:**

1. Add a module-level constant: `const DEV_SECRET = "dev-secret-do-not-use-in-production";`

2. In `requireAuth` (line 28), change:
   `jwt.verify(token, config.JWT_SECRET)` to `jwt.verify(token, config.JWT_SECRET || DEV_SECRET)`

3. In `optionalAuth` (line 57), change:
   `jwt.verify(token, config.JWT_SECRET)` to `jwt.verify(token, config.JWT_SECRET || DEV_SECRET)`

This makes verify consistent with sign in routes/auth.ts line 128, which already uses `config.JWT_SECRET || "dev-secret-do-not-use-in-production"`.

Do NOT change routes/auth.ts — it already has the correct fallback on sign.
  </action>
  <verify>
1. `npx tsc --noEmit` in server/ passes.
2. `cd /Users/blaisealbuquerque/Projects/awsHackathon/server && npx vitest run` — all existing tests pass.
3. Grep app.ts for `registerLimiter` and `loginLimiter` — both appear before authRouter.
4. Grep auth.ts for `DEV_SECRET` — appears in both requireAuth and optionalAuth verify calls.
  </verify>
  <done>Auth limiters wired in app.ts before authRouter (register at step 5, authRouter at step 6). JWT verify in both requireAuth and optionalAuth uses config.JWT_SECRET || DEV_SECRET, matching the sign fallback in routes/auth.ts.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd server && npx tsc --noEmit` passes with no errors.
2. Existing tests: `cd server && npx vitest run` — all tests pass (no regressions).
3. Rate limiter exports: rateLimiter.ts exports registerLimiter (3/min) and loginLimiter (10/min) with IP-keyed generators and distinct Redis prefixes.
4. Mount order: app.ts registers auth limiters on paths `/api/auth/register` and `/api/auth/login` BEFORE mounting authRouter.
5. JWT consistency: auth.ts uses `config.JWT_SECRET || DEV_SECRET` in both requireAuth and optionalAuth, matching the sign fallback in routes/auth.ts.
</verification>

<success_criteria>
- registerLimiter exported from rateLimiter.ts with limit: 3, windowMs: 60000, keyGenerator using req.ip
- loginLimiter exported from rateLimiter.ts with limit: 10, windowMs: 60000, keyGenerator using req.ip
- app.ts mounts both auth limiters before authRouter
- auth.ts defines DEV_SECRET and uses it in both jwt.verify calls
- All existing server tests pass
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-production-hardening/12-01-SUMMARY.md`
</output>
