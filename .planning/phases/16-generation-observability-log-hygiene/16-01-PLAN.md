---
phase: 16-generation-observability-log-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/services/videoGenerator.ts
  - server/src/services/musicService.ts
  - server/src/index.ts
  - server/src/services/redis.ts
autonomous: true

must_haves:
  truths:
    - "Video generation poll loop logs attempt number, elapsed time, and MiniMax status on every poll iteration"
    - "Music generation logs a progress event at the 30-second mark during the blocking API call"
    - "Config warnings for Redis and JWT do not appear in dev-mode startup logs"
    - "Redis console.warn for missing REDIS_URL does not appear in dev-mode startup"
    - "All new log entries use logEvent() with structured JSON format (event/timestamp/level fields)"
  artifacts:
    - path: "server/src/services/videoGenerator.ts"
      provides: "Poll-loop progress logging"
      contains: "video.poll_progress"
    - path: "server/src/services/musicService.ts"
      provides: "30-second progress timer logging"
      contains: "music.generation_progress"
    - path: "server/src/index.ts"
      provides: "Dev-mode config warning suppression for Redis and JWT"
    - path: "server/src/services/redis.ts"
      provides: "Dev-mode console.warn suppression for missing REDIS_URL"
  key_links:
    - from: "server/src/services/videoGenerator.ts"
      to: "server/src/services/logger.ts"
      via: "logEvent import"
      pattern: "logEvent.*video\\.poll_progress"
    - from: "server/src/services/musicService.ts"
      to: "server/src/services/logger.ts"
      via: "logEvent import"
      pattern: "logEvent.*music\\.generation_progress"
---

<objective>
Add progress logging to video and music generation poll/wait loops, and suppress noisy dev-mode config warnings for optional services (Redis, JWT).

Purpose: Video generation polls MiniMax up to 18 times with zero log output between task submission and completion. Music generation blocks for ~55s with no intermediate logging. Redis/JWT config warnings fire every startup even in dev where these are intentionally absent. This phase closes all four observability gaps.

Output: 4 modified files with ~20 lines of changes total.
</objective>

<execution_context>
@/Users/blaisealbuquerque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blaisealbuquerque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/src/services/logger.ts
@server/src/services/videoGenerator.ts
@server/src/services/musicService.ts
@server/src/index.ts
@server/src/services/redis.ts
@server/src/services/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add progress logging to video and music generation</name>
  <files>
    server/src/services/videoGenerator.ts
    server/src/services/musicService.ts
  </files>
  <action>
**videoGenerator.ts** — Inside the `runGeneration` function's `while` poll loop (line ~165), add a poll attempt counter (`let attempt = 0` before the loop, `attempt++` at the top of each iteration). After the `pollRes.json()` parse (after line ~183), add a `logEvent` call:

```typescript
logEvent("info", "video.poll_progress", {
  scene,
  taskId,
  attempt,
  maxAttempts: Math.floor(GENERATION_TIMEOUT_MS / POLL_INTERVAL_MS),
  status: pollJson.status ?? "unknown",
  elapsedMs: Date.now() - pollStart,
});
```

Place this AFTER the fetch response is parsed (so `pollJson.status` is available) but BEFORE the status checks (Fail/Success). This satisfies success criterion 1: "video poll attempt 3/18, status: processing, elapsed: 30s".

**musicService.ts** — Inside `runGeneration`, BEFORE the `fetch("https://api.minimax.io/v1/music_generation", ...)` call (line ~101), add:

```typescript
const progressTimer = setInterval(() => {
  logEvent("info", "music.generation_progress", {
    mood,
    elapsedMs: Date.now() - apiStart,
    phase: "awaiting_api_response",
  });
}, 30_000);
```

Note: `apiStart` is already declared at line 100. Move the `const apiStart = Date.now()` line ABOVE the `setInterval` so it's available.

In the `finally` block of the `try/catch` (around line 225), add `clearInterval(progressTimer)` BEFORE `entry.generating = false`. The `progressTimer` variable must be declared in the outer scope of `runGeneration` (before the `try`) so `finally` can access it. Declare it as `let progressTimer: ReturnType<typeof setInterval> | undefined;` in the function scope, then assign inside the `try`.

This satisfies success criterion 2: "Music generation logs periodic progress during the MiniMax API call (start, 30s mark, completion)". The start and completion are already logged by existing `music.generation_started` and `music.generation_completed` events.
  </action>
  <verify>
Run `npx tsc --noEmit` from `server/` to verify no type errors. Grep for `video.poll_progress` in videoGenerator.ts and `music.generation_progress` in musicService.ts to confirm the log events exist. Verify `clearInterval(progressTimer)` exists in musicService.ts's finally block.
  </verify>
  <done>
videoGenerator.ts logs attempt number, elapsed time, and MiniMax status on every poll iteration. musicService.ts logs a progress event every 30 seconds during the blocking API call with proper cleanup via clearInterval in finally.
  </done>
</task>

<task type="auto">
  <name>Task 2: Suppress dev-mode config warnings for optional services</name>
  <files>
    server/src/index.ts
    server/src/services/redis.ts
  </files>
  <action>
**index.ts** — Wrap the two `warnOnBlankConfig` calls for Redis (line 30-33) and JWT (line 34-37) inside a `if (config.NODE_ENV === "production")` guard:

```typescript
if (config.NODE_ENV === "production") {
  warnOnBlankConfig(
    ["REDIS_URL"],
    "Redis (needed for Phase 9 persistence)"
  );
  warnOnBlankConfig(
    ["JWT_SECRET"],
    "JWT Auth (needed for Phase 9 authentication)"
  );
}
```

Keep the other `warnOnBlankConfig` calls (AWS, Datadog, MiniMax, Neo4j) unchanged — those are always useful regardless of environment.

**redis.ts** — In `connectRedis()`, replace the `console.warn(...)` (line 24-26) with a `logEvent` call that uses structured logging instead of raw console.warn. Import `logEvent` from `./logger.js`. Use level "info" (not "warn") since this is expected behavior in dev:

```typescript
import { logEvent } from "./logger.js";
// ...
logEvent("info", "redis.skipped", {
  reason: "REDIS_URL not configured",
  fallback: "in-memory",
});
```

This eliminates the noisy `[redis] REDIS_URL not configured` console.warn in dev mode while still logging it as structured JSON at info level (visible if needed, not alarming). This satisfies success criteria 3 and 4.
  </action>
  <verify>
Run `npx tsc --noEmit` from `server/` to verify no type errors. Run existing tests with `npm test --prefix server` (all 41 should pass). Grep `index.ts` for `NODE_ENV.*production` to confirm the guard exists. Grep `redis.ts` for `logEvent` to confirm structured logging is used.
  </verify>
  <done>
Redis and JWT config warnings only appear in production. Redis missing-URL message uses structured logEvent at info level instead of console.warn. Dev-mode startup log noise reduced by eliminating 3 warning lines (2 from warnOnBlankConfig + 1 from redis.ts console.warn).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` in server/ passes with no errors
2. `npm test --prefix server` passes all existing tests
3. `grep -n "video.poll_progress" server/src/services/videoGenerator.ts` returns a match
4. `grep -n "music.generation_progress" server/src/services/musicService.ts` returns a match
5. `grep -n "clearInterval" server/src/services/musicService.ts` returns a match in the finally block
6. `grep -n "NODE_ENV.*production" server/src/index.ts` returns a match guarding Redis/JWT warnings
7. `grep -n "logEvent" server/src/services/redis.ts` returns a match replacing console.warn
</verification>

<success_criteria>
- Video poll loop emits structured JSON log with attempt/maxAttempts/status/elapsedMs on every iteration
- Music generation emits structured JSON log at 30-second intervals during blocking API call
- Dev-mode startup no longer shows Redis/JWT warnOnBlankConfig warnings
- Dev-mode startup no longer shows Redis console.warn for missing REDIS_URL
- All new log entries use logEvent() with event/timestamp/level fields (no raw console.warn/log)
- All 41 existing server tests continue to pass
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/16-generation-observability-log-hygiene/16-01-SUMMARY.md`
</output>
