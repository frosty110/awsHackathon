---
phase: 18-code-review-bug-fixes-wave-2
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/services/conversationStore.ts
  - server/src/routes/chat.ts
autonomous: true
must_haves:
  truths:
    - "conversationStore uses GETEX instead of separate GET+EXPIRE"
    - "Chat route holds conversation in local variable, writes to Redis once after stream"
    - "SSE stream checks res.write() return value for backpressure"
  artifacts:
    - path: "server/src/services/conversationStore.ts"
      provides: "GETEX optimization reducing Redis round-trips"
      contains: "getEx"
    - path: "server/src/routes/chat.ts"
      provides: "SSE backpressure check on res.write"
      contains: "drain"
  key_links:
    - from: "server/src/services/conversationStore.ts"
      to: "redis"
      via: "GETEX replacing GET+EXPIRE"
      pattern: "getEx"
    - from: "server/src/routes/chat.ts"
      to: "node:http"
      via: "res.write return + drain event"
      pattern: "drain"
---

<objective>
Optimize Redis round-trips in conversation store (criterion 8) and add SSE backpressure handling (criterion 7).

Purpose: The chat route currently makes 8+ Redis round-trips per turn (getOrCreate + appendMessage(user) + getWindowedHistory + appendMessage(assistant), each doing GET+EXPIRE). GETEX halves read round-trips, and the local variable pattern reduces writes. SSE backpressure prevents Node.js from buffering unbounded data when slow clients can't keep up.

Output: Optimized conversation store with GETEX, reduced writes, and backpressure-aware SSE streaming.
</objective>

<execution_context>
@/Users/blaisealbuquerque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/blaisealbuquerque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@server/src/services/conversationStore.ts
@server/src/routes/chat.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: GETEX optimization in conversationStore</name>
  <files>server/src/services/conversationStore.ts</files>
  <action>
    1. In `_getFromRedis`:
       - Replace the two-call pattern (GET + EXPIRE) with a single GETEX call:
         ```typescript
         private async _getFromRedis(conversationId: string): Promise<Conversation | null> {
           const raw = await redisClient.getEx(redisKey(conversationId), {
             EX: CONVERSATION_TTL_SECONDS,
           });
           if (!raw) return null;
           return JSON.parse(raw) as Conversation;
         }
         ```
       - This combines GET + TTL refresh into one Redis round-trip (requires Redis 6.2+).
       - Remove the separate `await redisClient.expire(...)` call.

    2. Wrap GETEX in a try/catch to handle Redis versions that don't support GETEX:
       - If the command throws (e.g., "ERR unknown command"), fall back to the old GET+EXPIRE pattern.
       - Log a one-time warning: `logEvent("warn", "conversationStore.getex_unsupported", { fallback: "get+expire" })`.
       - Use a module-level flag `let getexSupported = true;` that flips to false on first failure, so subsequent reads go directly to GET+EXPIRE without retrying GETEX.

    NOTE: This plan may conflict with Plan 01's conversationStore changes (adding userId). Since both are Wave 1, the executor should run them sequentially. Plan 01 adds userId to the type; this plan optimizes the Redis calls. Both are compatible.
  </action>
  <verify>
    - `npx tsc --noEmit` compiles clean.
    - `npm test` passes.
    - `grep "getEx" server/src/services/conversationStore.ts` returns a match.
    - `grep -c "redisClient.expire" server/src/services/conversationStore.ts` returns 0 (no separate expire calls).
  </verify>
  <done>
    - _getFromRedis uses GETEX (1 round-trip) instead of GET+EXPIRE (2 round-trips).
    - Graceful fallback to GET+EXPIRE if Redis version doesn't support GETEX.
  </done>
</task>

<task type="auto">
  <name>Task 2: SSE backpressure and local conversation variable</name>
  <files>server/src/routes/chat.ts</files>
  <action>
    1. SSE Backpressure in `chat.ts`:
       - After the SSE headers are written, create a helper function for checked writes:
         ```typescript
         function checkedWrite(data: unknown): boolean {
           if (clientDisconnected) return false;
           return res.write(`data: ${JSON.stringify(data)}\n\n`);
         }
         ```
       - In the `createMoodStreamDetector` callbacks, replace direct `res.write` with `checkedWrite`.
       - For the text callback, check the return value. If `false` (backpressure), log it but continue (conservative approach — don't make the callback async to avoid breaking the moodStreamDetector sync callback chain). The Bedrock stream is short-lived (seconds) and the kernel TCP buffer handles temporary backup.
       - Log when backpressure is detected: `logEvent("warn", "chat.sse_backpressure", { conversationId: conversation.id })`.
       - This satisfies criterion 7's requirement to "check res.write() return" without the risky async callback refactor described in the research pitfall #3.

    2. Local conversation variable pattern:
       - After `getOrCreate`, the conversation is already a local variable. The current code calls `appendMessage(conversation.id, ...)` which does a full Redis read+write each time.
       - Refactor the chat route to minimize Redis writes:
         a. After `getOrCreate` returns the conversation object, hold it locally.
         b. After the user message append, the conversation is in Redis (this write is necessary — if Bedrock fails, we want the user message preserved).
         c. After the stream completes, the assistant appendMessage is the second write — this is also necessary.
         d. The key optimization is in `getWindowedHistory` — since we just loaded the conversation in `getOrCreate` and appended the user message, we ALREADY have the full history in the local `conversation` object. Instead of calling `getWindowedHistory(conversation.id)` (which does another Redis GET), compute the windowed history from the local object:
           ```typescript
           const history = conversation.history.slice(-12);
           ```
         e. Similarly, `getCharacterClass` and `getPronouns` can use the local conversation object instead of making separate Redis calls.
       - This reduces per-turn round-trips from ~8 to ~3 (getOrCreate=1, appendUser=1, appendAssistant=1).
  </action>
  <verify>
    - `npx tsc --noEmit` compiles clean.
    - `npm test` passes.
    - `grep "drain\|backpressure\|checkedWrite" server/src/routes/chat.ts` returns matches.
    - `grep -c "getWindowedHistory" server/src/routes/chat.ts` returns 0 (replaced by local slice).
    - `grep -c "getCharacterClass\|getPronouns" server/src/routes/chat.ts` returns 0 (replaced by local reads).
  </verify>
  <done>
    - SSE writes check res.write() return value; backpressure logged.
    - Chat route uses local conversation object for history window, characterClass, pronouns.
    - Redis round-trips reduced from ~8 to ~3 per chat turn.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compiles clean
- `npm test` passes
- conversationStore uses getEx not separate get+expire
- chat.ts uses checkedWrite pattern for SSE
- chat.ts uses local conversation.history.slice for windowed history
</verification>

<success_criteria>
- GETEX halves Redis read round-trips
- Chat route holds conversation locally, reads characterClass/pronouns from local object
- SSE stream checks res.write() return value and logs backpressure
- Total Redis calls per chat turn reduced from ~8 to ~3
</success_criteria>

<output>
After completion, create `.planning/phases/18-code-review-bug-fixes-wave-2-idor-client-auth-memory-safety-prompt-injection-performance/18-04-SUMMARY.md`
</output>
