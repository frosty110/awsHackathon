{
  "buildCommand": "npm run build -w packages/shared-types && cd .. && yarn install && yarn build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
